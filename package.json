{
  "name": "zubora-gpt-extension",
  "version": "1.1.0",
  "description": "Zubora GPT Extension。どうせ誰も読まない説明文。",
  "author": "",
  "license": "MIT",
  "scripts": {
    "build:content": "vite build --config vite.config.content.ts",
    "build:background": "vite build --config vite.config.background.ts",
    "build:options": "vite build --config vite.config.options.ts",
    "build": "npm run build:content && npm run build:background && npm run build:options"
  },
  "dependencies": {
    "typescript": "^5.8.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "vite": "^5.4.19"
  }
}
